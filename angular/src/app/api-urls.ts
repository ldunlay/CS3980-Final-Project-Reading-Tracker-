export const base_url = 'http://localhost:8000';
export const auth_url = base_url + '/auth';
export const events_url = base_url + '/events';
export const users_url = base_url + '/users';

export const api_url = base_url + '/api';
export const signin_url = api_url + '/sign-in';
export const signup_url = api_url + '/signup';
export const current_books_url = api_url + '/current-books';
export const up_next_url = api_url + '/up-next';
